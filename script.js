const header=document.querySelector('.site-header');const toggle=document.querySelector('.menu-toggle');toggle?.addEventListener('click',()=>{const open=header.classList.toggle('open');toggle.setAttribute('aria-expanded',open);});document.querySelectorAll('nav a').forEach(link=>link.addEventListener('click',event=>{const section=document.querySelector(link.getAttribute('href'));if(!section)return;event.preventDefault();section.scrollIntoView({behavior:'smooth',block:'start'});history.replaceState(null,'',link.getAttribute('href'));header.classList.remove('open');toggle?.setAttribute('aria-expanded','false')}));const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}}),{threshold:.13});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));

const route=document.querySelector('.path-draw');const node=document.querySelector('.path-node');const pathCanvas=document.querySelector('.scroll-path');if(route&&node&&pathCanvas&&!matchMedia('(prefers-reduced-motion: reduce)').matches){const length=route.getTotalLength();route.style.strokeDasharray=length;let pending=false;const updateRoute=()=>{const documentHeight=document.documentElement.scrollHeight;pathCanvas.style.height=`${documentHeight}px`;const max=Math.max(1,documentHeight-innerHeight);const progress=Math.min(1,Math.max(0,scrollY/max));route.style.strokeDashoffset=length*(1-progress);const point=route.getPointAtLength(length*progress);node.setAttribute('cx',point.x);node.setAttribute('cy',point.y);node.style.opacity=progress>.01&&progress<.995?'1':'0';pending=false};const scheduleRoute=()=>{if(!pending){pending=true;requestAnimationFrame(updateRoute)}};addEventListener('scroll',scheduleRoute,{passive:true});addEventListener('resize',scheduleRoute);new ResizeObserver(scheduleRoute).observe(document.body);updateRoute()}
